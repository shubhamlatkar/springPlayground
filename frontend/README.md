# Generating and rendering react built with spring boot....

## Frontend react code under

```
/src/main/frontend
```

### Commands to run

```
> docker build -t service:app .
> docker run -p 8080:8080 service:app
```